%dw 2.0
output application/json
import * from dw::core::Dates
---
payload map (order) -> 
 order ++ {
  deliveryDuration: 
    daysBetween(
      (order.orderDate as Date ),
      (order.deliveredDate)
    )
}
